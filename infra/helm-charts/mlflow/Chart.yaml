apiVersion: v2
name: mlflow
description: MLflow tracking server for the distributed-workload PoC. Custom image
  wired to PostgreSQL (backend store) and S3/LocalStack (artifact root).
type: application
version: 0.1.0
appVersion: "1.0"
