{{- if .Values.enabled }}
apiVersion: v1
kind: Service
metadata:
  name: prefect-server
  labels:
    {{- include "prefect.labels" . | nindent 4 }}
    app: prefect-server
spec:
  type: ClusterIP
  selector:
    app: prefect-server
  ports:
    - name: http
      port: {{ .Values.port }}
      targetPort: http
{{- end }}
